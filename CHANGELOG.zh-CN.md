## NBT编辑器_1.0.1-更新日志

1. 自定义目录，不管是父目录还是直接的存档目录都允许加载，父目录会排除非存档，如果存档缺少其中一部分，拒绝加载，父目录则为不显示
例：
/storage/emulated/0/download/  允许的
/storage/emulated/0/download/{存档名}/  允许的
2. 增加防呆和防傻，如果用户将Blocktopograph被编辑过的存档，不管是移到旧目录还是新目录，或者是自定义目录，均无法加载玩家
3. 更新jar为稳定版，2019~2020的
4. 将部分NBT标签补回，并解决12 11 7是数组 其余为单组
5. ？应该没有人点那么快吧  防止用户点击重载过快，导致出现错误


## NBT编辑器_1.0.2-更新日志

1. 增加英语
2. 关于页面弹窗
3. 增加该类的说明 (物品ID自动匹配未实现)
4. 增加夜间和日间
5. UI重改
6. 重磅更新！ 将原有的iq80库更换成HiveGamesOSS，同时解决Blocktopograph锁问题，可以和Blocktopograph与澜诺无缝切换修改
7. 在编辑世界或者玩家身上的大量nbt导致闪退
8. 增加Blocktopograph类似树状图


## NBT编辑器_1.0.3-更新日志

新增

1. 增加 其他玩家NBT 更多操作与修改
2. 增加 编辑地图NBT 更多操作与修改
3. 增加 编辑村庄NBT 更多操作与修改


修复

1. 增加捕获那些通常导致“APP 已停止运行”的致命错误，并将报错信息保存到本地文件
2. 多线程分片读取，该目录下的db玩家存档
3. 解决新应用，没有旧应用文件夹使用权限 来自华为
4. 增加侧边栏，防止页面没地方放了
5. 任何时候加载数据时，都要精准更新
6. 修复，不管是公共目录还是私有目录，只要崩溃一次，不管是哪里的，两边都保存一次
7. 增加地图多线程，加快图片处理，防止用户输入的大小过大
8. 在该类其他玩家，编辑地图，甚至是村庄，里没有东西的时候，可以点击，虽然是空的，但是玩家可以自己创建
9. 在加载其他类的时候，当我点击全屏编辑却会被玩家给加载掉，虽然内容没丢失，不管是否有没有保存，一返回你一按重载，它就是玩家数据
10. 用户点击该类是可以自动加载，无需还在退一步到页面加载
11. 上次加载完存档a后，还保存写着世界，村庄等，应该自己在用户切换b存档后，自动重置
12. 如果加载过侧边栏的玩家，地图，村庄等，应该像是留在内存里，不用重载，不然每次都要重新加载
13. "加载存档"应该是直接加载已经选中的存档而不是跳出弹窗重新选
14. 在编辑任意nbt数据时，对于某些比较多的数据，可以使用搜索功能搜索到对应的类或类翻译
15. 在该玩家，地图，村庄 文件比较多的时候，可以选择使用搜索模糊去寻找
16. 在该玩家，地图，村庄 文件比较多的时候，有复选框可选几个删就几个
17. 主界面应该更紧凑
18. 增加该类的说明 (物品ID自动匹配实现) 需要重构
19. 我应该对这些ID进行更详细的分类，必须把它们各自分为一个文件
20. 先正常视图进入该层级，然后切换树形视图，会导致刷新，然后在正常视图进去，再切换，会重复进去
21. 列表模式的导航栈 (navigationStack) 存储的是“上一层的数据”（父节点），而不是“当前层的数据”
22. 视图同步的路径重复
23. 全屏上方的长条需要将主界面的功能复制过去
24. 用户的存档出现了损坏，会尝试进行修复，并告知用户是否需要修复，因为会丢失掉一些数据
25. Bridge 文件夹下放一个名为 .nomedia 的空文件，系统媒体服务就会直接忽略这个文件夹，不再尝试扫描和索引，从而彻底根除这个崩溃 来自OS2.0.203.0.VMLCNXM (Xiaomi Xiaomi 13T Pro/Redmi K60 Ultra)
26. 不管是公共还是私有，工作目录和备份目录，都会有.nomedia，防止国产系统魔改的系统媒体服务扫描跑去扫
27. 把搜索键值 ，原始类，世界类，还有旧的世界类，一些计数器，计分板等等
28. 寻找不到该键值内容，自动创建
29. 当加载到存档自动更换主页标题，放一些常用的显示地图名字，地图种子，可长按地图种子复制种子
30. 将多线程应用于所有读取，写入，当设备情况不允许时，设置有一个复选框，勾选默认为启用，不勾选则为单线程，这里的多线程不止存档，还有玩家，地图，村庄等等
31. 玩家不会看，而且还要记住这些随机复杂的地图文件夹名，不如遍历所有地图名
32. 更换应用签名，不再使用mt签名 需要创建一份属于该应用的签名
33. 在构建应用当中，必须有两种版本 一种是release正式版，一种是debug版本
34. 将资源进行英语翻译有中文和英文就已经足够 等侧边栏更改后(前置)，如果后期需要添加，之后再做决定
35. 多任务切换 + 自动草稿保存，直到你决定点击“保存”写入磁盘，或者换一个存档为止
36. 修复自定义拼图，国际化后无法使用问题
37. 修复主页大标题，不会切换为地图加种子
38. 防空指针闪退修复（针对搜索列表传 null 适配器的情况）